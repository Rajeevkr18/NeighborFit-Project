# MongoDB connection string - replace with your actual MongoDB URI
MONGODB_URI=mongodb://localhost:27017/neighborfit

# JWT secret key for token signing - use a strong, unique secret in production
JWT_SECRET=your_jwt_secret_key_here_make_it_long_and_secure

# Node environment (development, production, test)
NODE_ENV=development

# Server port
PORT=5000

# External API keys (add as needed for real data integration)
WALKABILITY_API_KEY=your_api_key_here
GOOGLE_MAPS_API_KEY=your_google_maps_api_key
